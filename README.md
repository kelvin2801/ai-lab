# ai-lab

## Scripts

### `dependency_check.py`
A generic script to check if required Python packages (defined within the script) are installed on the system. It reports missing packages to aid in ensuring the local development environment is set up properly for infrastructure experiments. Usage: `python3 scripts/dependency_check.py`.